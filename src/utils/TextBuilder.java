package utils;

public class TextBuilder {

	// overload 해서 다양한 버전...?
	public static String buildText() {
		// SystemMsg의 문자열 format들과 필요한 값들을 조합해 출력할 텍스트 반환
		return String.format(null, null);
	}
}
